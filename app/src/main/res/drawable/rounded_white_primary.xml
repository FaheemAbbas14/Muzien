<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color layer (transparent) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/colorPrimary" />
            <corners android:radius="16dp" /> <!-- Adjust the corner radius as needed -->
        </shape>
    </item>

    <!-- Border layer -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/white" />
            <stroke
                android:width="1dp"
                android:color="@color/colorPrimary" /> <!-- Adjust the border width and color as needed -->
            <corners android:radius="16dp" /> <!-- Match the corner radius of the background layer -->
        </shape>
    </item>
</layer-list>