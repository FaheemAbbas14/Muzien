<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color layer (transparent) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/colorPrimary" />
            <corners android:radius="100dp" /> <!-- Adjust the corner radius as needed -->
        </shape>
    </item>

    <!-- Border layer -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/colorPrimary" />
            <stroke
                android:width="2dp"
                android:color="@color/colorPrimary" /> <!-- Adjust the border width and color as needed -->
            <corners android:radius="100dp" /> <!-- Match the corner radius of the background layer -->
        </shape>
    </item>
</layer-list>